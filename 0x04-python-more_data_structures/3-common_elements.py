#!/usr/bin/python3
def common_elements(set_1, set_2):
    # Intersection() method returns a set that contains
    # the similarity between two or more sets.
    common = set_1.intersection(set_2)
    return (common)
